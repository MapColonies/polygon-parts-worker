{{- $tracingUrl := include "polygon-parts-worker.tracingUrl" . -}}
{{- $metricsUrl := include "polygon-parts-worker.metricsUrl" . -}}
{{- $serviceUrls := (include "common.serviceUrls.merged" .) | fromYaml }}

{{- if .Values.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-configmap" (include "polygon-parts-worker.fullname" .) }}
data:
  REQUEST_PAYLOAD_LIMIT: {{ .Values.env.requestPayloadLimit | quote }}
  RESPONSE_COMPRESSION_ENABLED: {{ .Values.env.responseCompressionEnabled | quote }}
  LOG_LEVEL: {{ .Values.env.logLevel | quote }}
  LOG_PRETTY_PRINT_ENABLED: {{ .Values.env.logPrettyPrintEnabled | quote }}
  {{ if .Values.env.tracing.enabled }}
  TELEMETRY_TRACING_ENABLED: 'true'
  TELEMETRY_TRACING_URL: {{ $tracingUrl }}
  {{ end }}
  {{ if .Values.env.metrics.enabled }}
  TELEMETRY_METRICS_ENABLED: 'true'
  TELEMETRY_METRICS_URL: {{ $metricsUrl }}
  {{ end }}
  JOB_MANAGER_BASE_URL: {{ $serviceUrls.jobManager | quote}}
  HEARTBEAT_BASE_URL: {{ $serviceUrls.heartbeatManager | quote}}
  POLYGON_PARTS_MANAGER_BASE_URL: {{ $serviceUrls.polygonPartsManager | quote}}
  POLYGON_PARTS_TYPE: {{ .Values.env.permittedTypes.tasks.polygonParts }}
  INGESTION_NEW_TYPE: {{ .Values.env.permittedTypes.jobs.new.type }}
  INGESTION_UPDATE_TYPE: {{ .Values.env.permittedTypes.jobs.update.type }}
  INGESTION_SWAP_UPDATE_TYPE: {{ .Values.env.permittedTypes.jobs.swapUpdate.type }}
  npm_config_cache: /tmp/
{{- end }}
